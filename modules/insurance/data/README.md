# Provenance database (Insurance mockup)

This folder contains a row-level provenance table for every datum shown in the insurance mockup.

- `mockup_data_lineage.csv`: one row per datum (value) with its Drive address + exact location (tab/cell or document reference).

